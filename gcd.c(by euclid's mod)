#include<stdio.h>
 int gcd(int x,int y)
 {
 	if(y==0)
 	{
 		return x ;
 	}
 	else
 	{
 		return gcd(y,x%y) ;
 	}
 }
 int main()
 {
	printf(" enter two integers \n") ;
	int x,y ;
	scanf("%d",&x) ;
	scanf("%d",&y) ;
	printf("the gcd of the integers is %d  ",gcd(x,y)) ;
 }
