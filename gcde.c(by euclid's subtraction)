#include<stdio.h>
 int gcd(int x,int y)
 {
 	if(x==y)
 	{
 		return x ;
 	}
 	else if(x>y)
 	{
 	 	return gcd(x-y,y) ;
 	}
 	else
 	{
 		return gcd(y-x,x) ;
 	}
 }
 int main()
 {
	printf(" enter two integers \n") ;
	int x,y ;
	scanf("%d",&x) ;
	scanf("%d",&y) ;
	printf("the gcd of the integers is %d  ",gcd(x,y)) ;
 }
	
